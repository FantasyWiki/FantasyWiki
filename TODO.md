## UI
### ArticleDetail
#### Header
remove the header completly and add it to the wikimedia summary card (remove the tier badge)
and make the summarry description collapse by default and expand on click.
The default layout should be the thumbnail with the title big on the right and under the descprition collapsed by default

## UX
### TeamManagement
Add a return to dashboard button with an arrow in the top left
