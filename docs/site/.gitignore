# Generated by scripts/prepare.mjs and vitepress: the markdown mirror, the
# derived JSON, the coverage reports CI downloads, and the built site. The
# repository root already ignores `build/` by name; this is here so the reason
# is written down next to the thing it ignores.
build/

# `npm run snapshot` renders the Atlas for the printed report. A checked-in copy
# would be stale the moment anyone wrote a link.
atlas-snapshot.svg
