import { describe, it, expect } from 'vitest'; import { mount } from
'@vue/test-utils'; import router from "@/router/index"; import
{{name}}
from '@/views/{{name}}.vue'; describe('{{name}}.vue', () => { it('should mount
without any console errors or warnings', () => { router.push("/");
router.isReady().then(() => { const wrapper = mount({{name}}, { global: {
plugins: [router], }, }); expect(wrapper.exists()).toBe(true); }); }) })